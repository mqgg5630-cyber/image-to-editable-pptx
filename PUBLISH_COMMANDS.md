# Publish commands (first GitHub release)

## Suggested tag

- `v0.1.0`

## Local commands

```bash
git init
git add .
git commit -m "chore: first public release v0.1.0"
git branch -M main
git remote add origin git@github.com:algerchen2024/image-to-editable-pptx.git
git push -u origin main
git tag v0.1.0
git push origin v0.1.0
```

## Suggested GitHub Release title

`v0.1.0 - Initial public release`

## Suggested GitHub Release summary

Initial public clean-room release of Image to Editable PPTX. Includes the packaged skill, repository source bundle, SHA256 checksums, and bilingual editable-PPTX showcase examples.
